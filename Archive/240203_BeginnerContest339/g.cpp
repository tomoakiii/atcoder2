#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MODN = 4;

int main() {
    int N, Q;
    cin >> N;
    vector<ll> A(N);
    for (int i=0; i<N; i++){
        cin >> A[i];
    }
    cin >> Q;
    ll a, b, r;
    ll b0 = 0;
    while(Q--){
        cin >> alpha >> beta >> gamma;
        ll li = alpha^b0, ri=beta^b0, xi=gamma^b0;
        ll lft = 0, rgt = 10e9+1, ctr = (lft+rgt)/2;
        if ()
    }


    cout << cnt << endl;
}
